# WP Local Docker
npm install -g wp-local-docker

# Selenium standalone
npm install -g selenium-standalone
selenium-standalone install

# Snyk CLI > Test vulnerable npm modules in node project
npm install snyk -g
